import 'package:flutter/material.dart';

class ReadingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Placeholder for reading stories
    return Scaffold(
      appBar: AppBar(title: Text('Reading Library')),
      body: Center(
        child: Text('Story goes here (with read-aloud and comprehension checks).'),
      ),
    );
  }
}